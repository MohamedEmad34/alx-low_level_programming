#include "main.h"
#include <stdio.h>

/**
 * _strlen - Takes a pointer to an char and return length
 *
 * @str: An string
 *
 * Return: nothing.
 */
int _strlen(char *str)
{
	return (strlen(*str));
}

